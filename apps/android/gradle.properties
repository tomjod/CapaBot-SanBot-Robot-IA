org.gradle.java.home=/home/mv/.jdks/temurin-8

# Project stays on the old Support libraries; compileSdk 26 is fixed by the
# Sanbot robot platform and AndroidX (appcompat 1.x) requires compileSdk 28+.
android.useAndroidX=false
android.enableJetifier=false
